# サーバーへのデプロイ手順（パスワード認証）

## 現在の状況

ビルドは完了していますが、サーバーへのSSH接続に認証が必要です。

## 📦 ビルド成果物

- **場所**: `/home/itoshu2/apps/shuke-b/scheduleboard/suke/build/`
- **サイズ**: 約1.8MB
- **ベースパス**: `/shuke-b/` （設定済み）

## 🚀 デプロイ方法

### 方法1: 手動デプロイスクリプト（推奨）

パスワードを入力してデプロイ：

```bash
cd /home/itoshu2/apps/shuke-b/scheduleboard
./MANUAL_DEPLOY.sh
```

**注意**: パスワードを3回入力する必要があります：
1. ディレクトリ作成時
2. ファイル転送時
3. 権限設定時

### 方法2: 手動コピー

#### ステップ1: サーバーにディレクトリを作成

```bash
ssh root@zatint1991.com "mkdir -p /var/www/html/shuke-b"
```

#### ステップ2: ファイルを転送

```bash
cd /home/itoshu2/apps/shuke-b/scheduleboard
scp -r suke/build/* root@zatint1991.com:/var/www/html/shuke-b/
```

#### ステップ3: 権限を設定

```bash
ssh root@zatint1991.com "chown -R www-data:www-data /var/www/html/shuke-b && chmod -R 755 /var/www/html/shuke-b"
```

### 方法3: ローカルから直接アップロード

もし、別のマシン（Windows/Mac）からアップロードする場合：

1. `suke/build/` フォルダ全体を圧縮
```bash
cd /home/itoshu2/apps/shuke-b/scheduleboard/suke
tar -czf shuke-b-build.tar.gz build/
```

2. FTPクライアント（FileZilla等）でサーバーにアップロード

3. サーバー上で展開
```bash
ssh root@zatint1991.com
cd /var/www/html
mkdir -p shuke-b
cd shuke-b
tar -xzf ~/shuke-b-build.tar.gz --strip-components=1
chown -R www-data:www-data /var/www/html/shuke-b
chmod -R 755 /var/www/html/shuke-b
```

## 🔧 nginx設定

ファイルのアップロード後、nginx設定を更新してください。

### ステップ1: サーバーにログイン

```bash
ssh root@zatint1991.com
```

### ステップ2: nginx設定を編集

```bash
sudo nano /etc/nginx/sites-available/default
```

### ステップ3: 以下の設定を追加

```nginx
    # shuke-b アプリケーション用のパス
    location /shuke-b {
        alias /var/www/html/shuke-b;
        try_files $uri $uri/ /shuke-b/index.html;
        
        # キャッシュ制御
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
```

**重要**: 既存の `server { ... }` ブロック内に追加してください。

### ステップ4: nginx設定をテスト

```bash
sudo nginx -t
```

### ステップ5: nginxをリロード

```bash
sudo systemctl reload nginx
```

## ✅ 確認

ブラウザで以下のURLにアクセス：

```
http://zatint1991.com/shuke-b/
```

アプリケーションが表示されれば成功です！

## 🔍 トラブルシューティング

### ファイルが存在するか確認

```bash
ssh root@zatint1991.com "ls -la /var/www/html/shuke-b/"
```

### nginx エラーログを確認

```bash
ssh root@zatint1991.com "tail -50 /var/log/nginx/error.log"
```

### 権限を再設定

```bash
ssh root@zatint1991.com "chown -R www-data:www-data /var/www/html/shuke-b && chmod -R 755 /var/www/html/shuke-b"
```

## 📝 補足: SSH鍵認証の設定（今後のため）

今後のデプロイを簡単にするため、SSH鍵認証を設定することをお勧めします：

```bash
# ローカルでSSH鍵を生成（既にある場合はスキップ）
ssh-keygen -t rsa -b 4096

# 公開鍵をサーバーにコピー
ssh-copy-id root@zatint1991.com

# 確認
ssh root@zatint1991.com "echo 'SSH鍵認証成功'"
```

これで次回から `./deploy.sh` が自動で動作します。

