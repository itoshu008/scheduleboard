// UI constants used across schedule components

export const SLOT_MINUTES = 15;

// Grid sizing
export const CELL_WIDTH_PX = 20; // width of one 15-min slot

// Monthly view
export const MONTH_CELL_HEIGHT_PX = 42; // height of one day row
export const MONTH_BAR_HEIGHT_PX = 42; // visual bar height in monthly grid

// Daily view (unified with monthly)
export const DAILY_ROW_HEIGHT_PX = 40; // height of one employee row (unified with monthly)
export const DAILY_BAR_HEIGHT_PX = 36; // visual bar height in daily grid (unified with monthly)

// Interaction thresholds
export const VERTICAL_SNAP_THRESHOLD_RATIO = 0.5; // ignore small vertical moves under half cell
