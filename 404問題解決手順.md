# 404エラー問題解決手順

## 🔍 問題の原因

nginx設定で `/shuke-b/` のlocationブロックが正しく動作していない可能性があります。

## ✅ 実施した対策

1. `/etc/nginx/sites-enabled/scheduleboard.conf` に `/shuke-b/` locationブロックを追加
2. alias設定: `/var/www/html/shuke-b/`
3. try_files設定: SPAルーティング対応

## 🔧 次の確認ステップ

### 1. ファイルの存在確認

```bash
ls -la /var/www/html/shuke-b/index.html
ls -la /var/www/html/shuke-b/static/js/main.e389dcba.js
```

### 2. nginxエラーログの確認

```bash
sudo tail -50 /var/log/nginx/error.log | grep -i "shuke-b"
```

### 3. アクセスログの確認

```bash
sudo tail -50 /var/log/nginx/access.log | grep "shuke-b"
```

### 4. nginx設定のテスト

```bash
sudo nginx -t
```

### 5. 手動でnginx設定を確認

```bash
sudo cat /etc/nginx/sites-enabled/scheduleboard.conf | grep -A 20 "shuke-b"
```

## 💡 代替案

### 案A: rootディレクトリを使う（他のアプリに影響する可能性あり）

```nginx
location /shuke-b/ {
    root /var/www/html;
    try_files $uri $uri/ /shuke-b/index.html;
}
```

### 案B: シンボリックリンクを使う

```bash
sudo ln -s /var/www/html/shuke-b /var/www/scheduleboard/shuke-b
```

そして、rootディレクトリ（`/var/www/scheduleboard`）から直接アクセス

### 案C: 別のサーバーブロックを作成

`/etc/nginx/sites-available/shuke-b.conf` を作成して、専用のserverブロックを設定

## 📝 現在の設定

```nginx
location /shuke-b/ {
    alias /var/www/html/shuke-b/;
    try_files $uri $uri/ /shuke-b/index.html;
    
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    location ~* \.html$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        expires 0;
    }
}

location = /shuke-b {
    return 301 /shuke-b/;
}
```

## 🚨 重要: sudoパスワードが必要

nginx設定の変更とリロードにはsudo権限が必要です：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 📍 アクセスURL

- **HTTPS**: https://zatint1991.com/shuke-b/
- **HTTP**: http://zatint1991.com/shuke-b/ （301リダイレクトでHTTPSに）

